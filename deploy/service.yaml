apiVersion: v1
kind: Service
metadata:
  name: romanian-folk-fight
  namespace: romanian-folk-fight
  labels:
    app: romanian-folk-fight
spec:
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30089
    name: http
  selector:
    app: romanian-folk-fight
  type: NodePort
